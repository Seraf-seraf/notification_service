Роль: архитектор распределнных систем, devops, php разработчик(laravel octane).
БД: постгрес
Кэш: редис
Брокер сообщений: рэббит

Задачи в файле tasks.txt

Техническое задание:
Микросервис уведомлений (Notification Service)
Цель этого задания - посмотреть, как ты проектируешь распределенные системы,
работаешь с асинхронными процессами и обеспечиваешь качество кода через
автоматизированное тестирование.

Функциональные требования (Бизнес-требования)
- Массовая рассылка уведомлений: Система должна предоставлять API для
запуска массовой отправки SMS или Email-сообщений. Инициатор запроса
передает канал связи, текст сообщения и массив идентификаторов
получателей.
- Приоритезация трафика: Система должна гарантировать доставку
критичных сообщений без задержек. Транзакционные уведомления (коды
доступа, срочные изменения маршрутов) должны получать наивысший
приоритет и отправляться вне очереди, обгоняя маркетинговые рассылки.
- Детализация статусов доставки: Система должна предоставлять API для
запроса истории и текущего статуса всех уведомлений конкретного
подписчика. 
Статусы:
- в очереди (сообщение принято и ожидает отправки);
- отправлено (передано шлюзу/провайдеру);
- доставлено (подтверждено провайдером);
- отброшено (ошибка доставки, несуществующий номер/email и т.д.).

## Нефункциональные требования (Архитектура и качество)
### Гарантия доставки сообщений (Reliability):
- Персистентность: Использование брокеров сообщений для хранения очереди.
- Модель доставки: Поддержка семантики at-least-once. Огромным плюсом будет реализация exactly-once на уровне бизнес-логики.
- Retry-механизмы: Автоматический повтор попыток отправки при временной недоступности шлюзов.
- Дедубликация (Idempotency): Защита от повторной отправки одного и того же сообщения при дублировании запросов от вызывающего сервиса.
- Тестирование: Обязательное наличие интеграционных тестов на основные сценарии. 
  Мы хотим видеть автоматизированную проверку всей цепочки: от получения сообщения из очереди до корректного изменения статуса в базе данных и вызова нужного провайдера.

## Cloud Native и развертывание:
- Упаковка сервиса в Docker-образ.
Весь проект (БД, брокер, кэш, приложение) должен запускаться одной командой docker-compose up.

Примечание: для внешних шлюзов используй классы-заглушки (моки), которые имитируют работу реальных провайдеров.

Требования к стеку:
php 8.5, laravel octane (swoole), конкретные версии постгрес, redis, рэббит (latest запрещены).

Начать разработку с составления архетиктуры приложения, как взаимодейтсвует актер с сервисом. 
Авторизация вне ответсвенности приложения.

Далее описать спецификацию swagger.
Вести разработку через тестирование, сначала написать тесты, после писать реализацию.
Вести логирование запросов, создать мидлвару, в которойпринимать requestid в заголовке или генерировать свой и добавлять в заголовок, 
добавить middleware victoriametrics для сбора метрик - время выполения запроса по стандартным бакетам перцентилей.
Добавить в образ grafana с конкретной версией, сохранить дашборд в infra, чтобы он сразу был доступен при запуске образа всего приложения.
Приоритезацию сообщений передает api инициатор запроса - приоритет задавать числами от 1 (несрочный) до 3 (срочный). 
Так как в требованиях не указано, нужно ли ставить низкий приоритет на паузу или в очередь "на низком старте", то просто рассылать сообщения строго по приоритету. 

Моки провайдеров написать как простые сервера на go, которые будут асинхронно и с простой логикой обрабатывать сообщения - нужна поддержка статусов (смотреть функциональные требования).

Ожидаемая начальная структура проекта:

apps/
    servicenotification/
    smsprovider/
    emailprovider/
docs/
infra/ (файлы докера держать тут)
README.md
.gitignore
.dockerignore
.env.example
Makefile

Необходимо учесть требование к масштабированию системы, микросервис не должен зависеть напрямую от конкретных провайдеров обработки сообщений.
Коммиты писать по конвенции коммитов, на русском языке.
Не пиши док блоки в коде. Во всех классах использовать declare(strict_types=1).
Если файл в индексе git, а после в него внес изменения, то убирать его из индекса, чтобы я видел, что файл нужно проверить,
убирай только то что изменил снова. Если файл в индексе - я его проверил.